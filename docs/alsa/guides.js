/* Editorial learning material, separate from the imported ALSA reference. */
window.ALSA_GUIDES = [
  { id: 'guide/welcome', title: 'Start here', category: 'The field guide', summary: 'Your map to PCM audio: a learning path, the essentials, and the original ALSA reference.', html: `
    <div class="hero"><div class="eyebrow accent">A FIELD GUIDE TO ALSA PCM</div><h1>Audio, one frame<br>at a time<span class="accent">.</span></h1><p class="lead">From your first sound to the details of the C API.<br> A clearer path through Linux’s PCM interface.</p><div class="hero-actions"><a class="button primary" href="#/guide/getting-started">Play your first frames <span>→</span></a><a class="text-action" href="#/reference">Explore the API ↗</a></div></div>
    <div class="pipeline" role="img" aria-label="Playback: your application writes frames to a ring buffer; the audio device consumes them."><div class="pipeline-top"><span>THE PLAYBACK PATH</span><span>application → hardware</span></div><div class="pipeline-body"><div class="endpoint"><span class="diagram-icon">{ }</span><strong>Your application</strong><code>snd_pcm_writei()</code></div><span class="flow-arrow">→</span><div class="buffer"><div class="buffer-cells"><i></i><i></i><i></i><i></i><i></i><i></i><i></i><i></i></div><strong>Ring buffer</strong><span>frames waiting to play</span></div><span class="flow-arrow">→</span><div class="endpoint"><span class="diagram-icon">▥</span><strong>Audio device</strong><span>frames become sound</span></div></div><div class="pipeline-bottom"><span><b>PCM</b> · Pulse Code Modulation</span><span>Capture follows the reverse path.</span></div></div>
    <section id="find-your-path"><div class="section-heading"><h2>Find your starting point</h2><span>THREE WAYS IN</span></div><div class="path-cards"><a href="#/guide/getting-started"><span class="card-number">01 / BUILD</span><h3>Make a sound <span>↗</span></h3><p>Open a device, configure a stream, and write your first frames.</p><span class="card-link">Getting started →</span></a><a href="#/guide/mental-model"><span class="card-number">02 / UNDERSTAND</span><h3>Get the mental model <span>↗</span></h3><p>Samples, frames, periods, buffers. The vocabulary that makes it click.</p><span class="card-link">Core concepts →</span></a><a href="#/reference"><span class="card-number">03 / LOOK UP</span><h3>Find the right call <span>↗</span></h3><p>Search signatures, parameters, return values, and related functions.</p><span class="card-link">API reference →</span></a></div></section>
    <section id="smallest-model"><h2>The smallest useful mental model</h2><p>A PCM stream moves <strong>frames</strong> between your application and an audio device. You choose how those frames are represented, then keep the buffer supplied (playback) or emptied (capture).</p><div class="facts"><div><code>sample</code><p>One channel’s value at one instant.</p></div><div><code>frame</code><p>One sample for every channel at that instant.</p></div><div><code>period</code><p>A chunk associated with hardware progress and wakeups.</p></div></div><div class="callout"><strong>Frames ≠ bytes.</strong> For stereo S16 audio, one frame contains two 2-byte samples: <code>4 bytes</code>. Most PCM transfer counts are in frames.</div></section>
    <section id="reading-map"><h2>Less scrolling. More understanding.</h2><div class="reading-rows"><a href="#/guide/how-to"><span>How to use these docs</span><span>Guides, manual, and reference explained →</span></a><a href="#/manual/pcm_handshake"><span>Follow the stream lifecycle</span><span>Open → prepared → running →</span></a><a href="#/guide/recovery"><span>When audio goes wrong</span><span>Underruns, suspension, and recovery →</span></a></div></section>
    <div class="source-note"><strong>Same reference. A different way to read it.</strong><p>The PCM manual and API entries preserve upstream content. This field guide adds an editorial learning layer; it isn’t official ALSA documentation. <a href="#/guide/how-to">About this snapshot →</a></p></div>` },
  { id: 'guide/getting-started', title: 'Play your first frames', category: 'Getting started', summary: 'Install ALSA development headers, compile a complete C playback example, and understand open, configure, write, and drain.', html: `
    <p class="lead">A small, complete playback program. Start with blocking, interleaved I/O; explore lower-level tuning after sound works.</p>
    <div class="callout"><strong>Before you start</strong> You need Linux, a C compiler, and alsa-lib development headers. Turn your volume down before testing audio. The example plays a quiet 440 Hz tone for two seconds.</div>
    <h2 id="install">1. Install the tools</h2><p>On Debian or Ubuntu:</p><pre><code>sudo apt install build-essential pkg-config libasound2-dev alsa-utils</code></pre><p>On Fedora:</p><pre><code>sudo dnf install gcc pkgconf-pkg-config alsa-lib-devel alsa-utils</code></pre><p>Use <code>aplay -L</code> to list logical PCM names. Begin with <code>default</code>: it may route through a sound server or ALSA plugins. A raw <code>hw:0,0</code> device bypasses conversion and may be busy or reject your format.</p>
    <h2 id="lifecycle">2. Know the four calls</h2><ol class="steps"><li><strong><a href="#/api/snd_pcm_open">snd_pcm_open()</a></strong><p>Open a named PCM for playback. A mode of <code>0</code> selects blocking I/O.</p></li><li><strong><a href="#/api/snd_pcm_set_params">snd_pcm_set_params()</a></strong><p>Choose S16 little-endian, stereo, 48 kHz, interleaved access. This convenience function configures and prepares the stream. Its latency argument is in microseconds, not frames.</p></li><li><strong><a href="#/api/snd_pcm_writei">snd_pcm_writei()</a></strong><p>Send frames. A positive return can be smaller than requested: advance your buffer by the frames actually written and retry the remainder.</p></li><li><strong><a href="#/api/snd_pcm_drain">snd_pcm_drain()</a> + <a href="#/api/snd_pcm_close">snd_pcm_close()</a></strong><p>Let queued audio finish, then release the handle. Use <code>snd_pcm_drop()</code> instead when you want an immediate stop.</p></li></ol>
    <h2 id="run">3. Build and run</h2><p><a class="download-link" href="examples/playback.c" download>↓ Download playback.c</a> — a complete example with short-write handling, error recovery, and cleanup.</p><pre><code>cc -std=c11 -Wall -Wextra playback.c -o playback \\
  $(pkg-config --cflags --libs alsa) -lm
./playback</code></pre><p>Pass a different PCM name as the first argument. The <code>null</code> plugin exercises the program without producing sound:</p><pre><code>./playback null</code></pre>
    <details class="code-details"><summary>Read the complete example <span>C · playback.c</span></summary><pre><code id="playback-source"></code></pre></details>
    <h2 id="next">4. Go beyond the first sound</h2><p>The example favors clarity, not minimal latency. For explicit buffer and period control, use <a href="#/api/snd_pcm_hw_params_any">snd_pcm_hw_params_any()</a>, refine the hardware parameters, apply them, then adjust software thresholds.</p><ul><li><a href="#/guide/mental-model">Understand buffer sizing and frame math.</a></li><li><a href="#/manual/pcm_params">Read how hardware and software parameters differ.</a></li><li><a href="#/guide/recovery">Handle underruns without guessing.</a></li><li><a href="#/manual/pcm_examples">Explore upstream examples, including capture and mmap.</a></li></ul>` },
  { id: 'guide/mental-model', title: 'Samples, frames & buffers', category: 'Core concepts', summary: 'Frame vs sample, byte math, interleaved layout, periods, latency, and the PCM state machine.', html: `
    <p class="lead">Most PCM confusion comes from mixing up units. Keep samples, frames, bytes, and time separate.</p>
    <h2 id="units">Start with the units</h2><table><thead><tr><th>Term</th><th>Meaning</th><th>Stereo S16 example</th></tr></thead><tbody><tr><td>Sample</td><td>A value for one channel</td><td>2 bytes</td></tr><tr><td>Frame</td><td>One sample per channel</td><td>4 bytes</td></tr><tr><td>Rate</td><td>Frames per second</td><td>48,000 frames/s</td></tr><tr><td>Buffer</td><td>Capacity of the PCM ring</td><td>4,800 frames ≈ 100 ms</td></tr></tbody></table><pre><code>bytes = frames × channels × physical_bytes_per_sample
time_seconds = frames / rate</code></pre><p>Use the format’s <em>physical</em> width, not just its significant bits. For example, <code>S24_LE</code> uses a 32-bit container, while <code>S24_3LE</code> is packed into 3 bytes. See <a href="#/manual/pcm_formats">PCM formats</a>.</p>
    <h2 id="layout">Interleaved or non-interleaved?</h2><div class="layout-demo"><span>INTERLEAVED · one buffer</span><code><b>L₀</b> R₀ <b>L₁</b> R₁ <b>L₂</b> R₂ <b>L₃</b> R₃</code></div><p><a href="#/api/snd_pcm_writei">snd_pcm_writei()</a> consumes interleaved frames. <a href="#/api/snd_pcm_writen">snd_pcm_writen()</a> takes an array of channel-buffer pointers: one buffer of left samples, one of right samples, and so on. The configured access mode must match the transfer method.</p>
    <h2 id="buffer-period">A buffer is not a period</h2><p>The ring buffer holds frames in flight. A period is a hardware processing interval, commonly associated with progress notifications. Smaller periods usually mean more frequent wakeups; a smaller buffer leaves less time to recover from scheduling delays.</p><p><code>avail_min</code> is the software wakeup threshold. It is distinct from the hardware period size. Don’t assume one wakeup means exactly one period must be written, or that a write must equal a period.</p><div class="callout"><strong>Capacity is not end-to-end latency.</strong> Buffer duration is useful for reasoning, but queued frames, plugins, sound servers, and the device affect actual latency. Consult <a href="#/api/snd_pcm_delay">snd_pcm_delay()</a> and the device’s timing behavior.</div>
    <h2 id="states">Think in states</h2><div class="state-flow"><code>OPEN</code><span>→</span><code>PREPARED</code><span>→</span><code>RUNNING</code></div><p>A successful <code>snd_pcm_hw_params()</code> installs the configuration and calls prepare. A prepared playback stream normally starts once its start threshold is reached. An underrun enters <code>XRUN</code>; you must recover before continuing.</p><p>Read the <a href="#/manual/pcm_handshake">full state descriptions</a> and <a href="#/manual/pcm_action">state-changing calls</a>. Pause and resume capabilities depend on hardware; don’t assume they are available.</p>` },
  { id: 'guide/recovery', title: 'Errors & recovery', category: 'Practical guides', summary: 'What EPIPE, EAGAIN, ESTRPIPE and ENODEV mean, and how to recover a PCM stream safely.', html: `
    <p class="lead">An audio stream is a live system. Check every return value, track partial progress, and make recovery part of the transfer loop.</p>
    <h2 id="return-values">Read return values correctly</h2><p>PCM I/O calls return a signed frame count or a negative error code. Keep the result in <code>snd_pcm_sframes_t</code> until you have checked it. Do not cast a negative error to an unsigned count, and don’t use <code>errno</code> in place of the returned ALSA error.</p><pre><code>snd_pcm_sframes_t n = snd_pcm_writei(pcm, data, frames);
if (n &lt; 0) {
    fprintf(stderr, "%s\\n", snd_strerror((int)n));
}</code></pre>
    <h2 id="errors">Common errors, useful responses</h2><table><thead><tr><th>Error</th><th>What to do</th></tr></thead><tbody><tr><td><code>-EAGAIN</code></td><td>No frames can move yet in nonblocking mode. Wait with poll or <code>snd_pcm_wait()</code>, then retry; don’t busy-spin.</td></tr><tr><td><code>-EPIPE</code></td><td>Playback underrun or capture overrun. Use <code>snd_pcm_recover()</code>, check its result, then resume transfers.</td></tr><tr><td><code>-ESTRPIPE</code></td><td>Stream suspended. The recovery helper can attempt resume and fall back to prepare.</td></tr><tr><td><code>-EBADFD</code></td><td>Wrong PCM state. Inspect your lifecycle, rather than blindly retrying.</td></tr><tr><td><code>-ENODEV</code></td><td>Device unavailable or removed. Stop using the handle; arrange cleanup and reopen when appropriate.</td></tr><tr><td><code>-ENODATA</code></td><td>An external dependency is missing. The upstream manual requires a complete PCM restart; the recovery helper cannot fix this.</td></tr></tbody></table>
    <h2 id="recovery">Recovery is not lossless</h2><p><a href="#/api/snd_pcm_recover">snd_pcm_recover()</a> handles <code>-EINTR</code>, <code>-EPIPE</code>, and <code>-ESTRPIPE</code>; other errors are returned unchanged. Check its return value. Preparing a stream after an xrun resets it; don’t assume the audio timeline stayed continuous.</p><div class="callout"><strong>Fix the cause, too.</strong> Repeated underruns often mean the producer is late. Avoid allocations, file I/O, and long locks in the audio loop. Start with a more forgiving buffer, then measure before reducing it.</div>
    <h2 id="polling">Poll through ALSA</h2><p>Obtain descriptors through <a href="#/api/snd_pcm_poll_descriptors">snd_pcm_poll_descriptors()</a> and interpret results with <a href="#/api/snd_pcm_poll_descriptors_revents">snd_pcm_poll_descriptors_revents()</a>. Don’t assume descriptor direction directly matches playback or capture: plugins may change the mapping.</p><p>See the complete <a href="#/manual/pcm_errors">upstream error-code chapter</a> and the <a href="#/guide/getting-started">working playback loop</a>.</p>` },
  { id: 'guide/how-to', title: 'How to use these docs', category: 'About the guide', summary: 'Navigate the learning guides, original PCM manual and searchable C API. Scope, provenance, offline usage, and keyboard shortcuts.', html: `
    <p class="lead">Three layers, each with a different job. Learn the concepts here; use the upstream reference for exact API contracts.</p>
    <h2 id="layers">Pick the right layer</h2><dl><dt>Field guide · editorial</dt><dd>The pages under “Learn” are newly written explanations and a practical example. They give you a path through the API, not a replacement specification.</dd><dt>PCM manual · upstream</dt><dd>The full linked PCM overview, broken into navigable chapters. Original wording is retained, including historical descriptions. Every chapter links back to its original location.</dd><dt>API reference · upstream</dt><dd>Individual documented members from the PCM interface and its direct topic pages: signatures, parameter descriptions, return values, enumerations, and cross-references.</dd></dl>
    <h2 id="lookup">Look up a function</h2><p>Press <kbd>/</kbd> or <kbd>Ctrl K</kbd> (<kbd>⌘ K</kbd> on macOS) and type a symbol, topic, or error. You can omit <code>snd_pcm_</code>: try <code>writei</code>, <code>buffer size</code>, or <code>EPIPE</code>. Use arrow keys and Enter, or click a result.</p><p>In the <a href="#/reference">API browser</a>, filter by topic or type a symbol. A function’s parameter table describes inputs and outputs; its return section tells you whether a result is an error, a count, or a value. Follow related links to understand the required stream state.</p>
    <h2 id="scope">Scope & provenance</h2><p>This is an independent reader for <strong>alsa-lib’s C PCM interface</strong>, not all of ALSA. It includes the PCM manual and documented members from 17 API topic pages. Structure definitions, source listings, extended plugin subtopics, and upstream example pages that aren’t imported open on alsa-project.org.</p><p>Upstream pages are a dated snapshot, not a guarantee of your installed library version. Check <code>pkg-config --modversion alsa</code> locally. Historical passages (for example, timestamp modes and asynchronous signals) may need to be read alongside current API entries. Original content remains attributed to the ALSA project and its contributors.</p><p><a href="https://www.alsa-project.org/alsa-doc/alsa-lib/pcm.html">Original PCM documentation ↗</a> · <a href="https://github.com/alsa-project/alsa-lib">alsa-lib source and licensing ↗</a></p>
    <h2 id="offline">Local-first, shareable links</h2><p>Imported text, search, waveform images, and the playback example are bundled locally. No fonts, analytics, CDN, or remote search service are needed. Links outside the snapshot require a network connection.</p><p>Every guide, manual chapter, and API entry has its own URL fragment. Copy your address bar to share a location on the same hosted site. Use the right-hand contents list to jump within a page. Your browser’s Back and Forward buttons work normally.</p>` }
];
